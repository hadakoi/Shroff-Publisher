import CartClient from "./CartClient";

export const metadata = {
  title: "Cart - Shroff Publishers",
  description: "Your shopping cart with Shroff Publishers.",
};

export default function CartPage() {
  return <CartClient />;
}
